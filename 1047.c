#include<stdio.h>
int main()
{
  int ini_hour, ini_min, final_hour, final_min;
  scanf("%d %d %d %d", &ini_hour, &ini_min, &final_hour, &final_min);

  a = final_hour-final_min;
  b = ini_min-final_min;

  printf("%d\n %d\n", a,b);

  for()
  return 0;
}
